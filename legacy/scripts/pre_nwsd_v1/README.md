# Pre-NWSD-v1 Script Archive

These scripts were superseded by the current desktop inference flow, NWSD-v1 training
workflow, or dataset-level evaluation tools. They are retained as source history only and
are not part of the active supported workflow.

The active commands live under `scripts/` and are documented in the repository README.
